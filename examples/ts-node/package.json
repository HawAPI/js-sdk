{
  "name": "ts-node",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "main": "/dist/index.js",
  "license": "MIT",
  "scripts": {
    "start": "node ./dist/index.js",
    "build": "yarn clean && yarn && yarn tsc",
    "dev": "yarn tsc && yarn start",
    "clean": "rm -rf ./dist/ ./node_modules/"
  },
  "dependencies": {
    "@hawapi/js-sdk": "../../"
  },
  "devDependencies": {
    "@types/node": "^20.4.1",
    "typescript": "^5.1.6"
  }
}
